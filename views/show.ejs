<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="/css/styles.css" rel="stylesheet"/>
  <title>Show Page</title>
</head>
<body>
  
  <p class="showTitle"><%=item.name; %> </p>
  <div class="showDetails"> 
    <img class="showImg" src = <%= item.img %> > </br>
      <p id="showDescription"> <%=item.description; %> </p> </br>
      <p id="showPrice">Price: $<%=item.price%> </p> </br>
      
      <% if(product.qty > 0){ %>
        <p id="priceRem"><%=item.qty%> in stock </p>
        <form action="/products/<%=item.id%>/buy?_method=PUT" method="POST">
          <input class ="buy" type="submit" value="BUY"/>
        </form>
    
        <% } else { %>
      <p class="outOfStock">"Out of Stock"</p>
          <% } %>
      <nav>
        <a href="/items/<%=item.id%>/edit" class="showEdit">Edit</a> </br>
   
      </nav>
  </div>
      <form action="/items/<%=item.id%>?_method=DELETE" method="POST">
        <input type="submit" value="DELETE" class="deleteBtn" />
      </form>
  <a href="/items" class="showBack">BACK</a>

</body>
</html>